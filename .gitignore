node_modules/
.expo/
dist/
web-build/
builds/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
google-play-service-account.json
.DS_Store
npm-debug.*

# dočasný výstup smoke testu webového buildu
web-smoke/

# lokalni konfigurace nastroju
.claude/

# Pozn.: CNAME je v repu DVAKRAT — v koreni (vytvoril ho GitHub sam pri
# nastaveni custom domain) a v docs/. Pages je nastavene na slozku /docs,
# takze plati `docs/CNAME`; ten v koreni je necinny. Nemazat ho — GitHub si
# ho spravuje sam a rucni zasah muze shodit custom domain.

# Supabase CLI — mezipaměť po `link`. Drží project-ref a verze služeb,
# nic tajného, ale do repozitáře nepatří.
supabase/.temp/
supabase/.branches/
